morsify for beaglebone black

start node webserver, initizlize webpage with inpuÄt, read the input, convert to morse code, lightup a gpio

---

## Code Companion (aXet.flows `.deptapp`)

The [`code-companion/`](code-companion/) directory holds the source,
build tooling and tests for the **Code Companion** SAP development
assistant â€” an aXet.flows application delivered as a `.deptapp` export.
It is unrelated to the Morse code project above and is self-contained.

- Importable build: `code-companion/build/aXet.SAP__Code_Agents_v4.7.0_export.deptapp`
- Start here: [`code-companion/README.md`](code-companion/README.md)
